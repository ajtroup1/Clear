class MyClass {
  pub property1
  property2

  fn MyClass(x, y) {
    property1 = x;
    property2 = y;
  }

  pub fn add() {
    let result = property1 + property2;

    return result;
  }
}

let instance = new MyClass(1, 2);
let result = instance.add();

return result;